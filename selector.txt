$pattern = '(?i)(In production|\.\.this would\.\.|for now|\.\.\.simple\.\.\.|mock|stub|simulation)'; $root = (Resolve-Path .).Path; $matches = Get-ChildItem crates/dchat-blockchain -Recurse -File | Select-String -Pattern $pattern; $matches | ForEach-Object { "$($_.Path):$($_.LineNumber):$($_.Line.TrimEnd())" } | Set-Content -Encoding utf8 crates/dchat-blockchain/placeholder_scan.txt; $byFile = $matches | Group-Object Path | Sort-Object Name; $md = New-Object System.Collections.Generic.List[string]; $md.Add('# dchat-blockchain placeholder phrase scan'); $md.Add(''); $md.Add('Pattern: `In production...`, `..this would..`, `for now`, `...simple...`, `mock`, `stub`, `simulation` (case-insensitive)'); $md.Add(''); foreach ($g in $byFile) { $rel = $g.Name.Substring($root.Length + 1).Replace('\','/'); $md.Add("## $rel"); foreach ($m in ($g.Group | Sort-Object LineNumber)) { $line = $m.Line.TrimEnd(); $ln = $m.LineNumber; $md.Add("- [$rel#L$ln]($rel#L$ln): $line"); } $md.Add(''); } $md | Set-Content -Encoding utf8 crates/dchat-blockchain/placeholder_scan.md$pattern = '(?i)(In production|\.\.this would\.\.|for now|\.\.\.simple\.\.\.|mock|stub|simulation)'; $root = (Resolve-Path .).Path; $matches = Get-ChildItem crates/dchat-blockchain -Recurse -File | Select-String -Pattern $pattern; $matches | ForEach-Object { "$($_.Path):$($_.LineNumber):$($_.Line.TrimEnd())" } | Set-Content -Encoding utf8 crates/dchat-blockchain/placeholder_scan.txt; $byFile = $matches | Group-Object Path | Sort-Object Name; $md = New-Object System.Collections.Generic.List[string]; $md.Add('# dchat-blockchain placeholder phrase scan'); $md.Add(''); $md.Add('Pattern: `In production...`, `..this would..`, `for now`, `...simple...`, `mock`, `stub`, `simulation` (case-insensitive)'); $md.Add(''); foreach ($g in $byFile) { $rel = $g.Name.Substring($root.Length + 1).Replace('\','/'); $md.Add("## $rel"); foreach ($m in ($g.Group | Sort-Object LineNumber)) { $line = $m.Line.TrimEnd(); $ln = $m.LineNumber; $md.Add("- [$rel#L$ln]($rel#L$ln): $line"); } $md.Add(''); } $md | Set-Content -Encoding utf8 crates/dchat-blockchain/placeholder_scan.md